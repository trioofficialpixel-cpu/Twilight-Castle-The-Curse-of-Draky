extends VideoStreamPlayer



@export var next_scene : PackedScene    # drag your menu scene here in inspector

func _ready():
	connect("finished", Callable(self, "_on_finished"))  # signal emitted when video ends

func _on_finished():
	get_tree().change_scene_to_packed(next_scene)
